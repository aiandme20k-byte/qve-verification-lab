# Architecture
React/TypeScript UI → FastAPI API → deterministic Python services → SQLite/provenance layer.
Gemini, when configured, is restricted to language tasks: extraction, explanation, translation and drafting.
Raw imports are immutable in concept and are never silently promoted to verified evidence.
